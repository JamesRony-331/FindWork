# Admin interface fidelity ledger

| Area | Accepted concept evidence | Implementation evidence | Fix or deviation |
| --- | --- | --- | --- |
| Login composition | Accepted composition and copy, normalized to a 1440×840 PNG. The raster divider is fixed at x=835: 835px left and 605px right, the nearest whole-pixel representation of 58/42. The form remains centered without a floating card or decorative badge. | Final screenshot `/private/tmp/admin-task7-login-1440-fix1.png` is 1440×840. Browser geometry measured the split at 835.19/604.81px, the illustration at y=300 with a 540px field, the centered form at 408px, and the inputs at 52px. The enlarged illustration now includes the accepted lower grid, briefcase, and resume details; visible desktop copy and the unchecked remember state remain exact. | Fix round 1 moved/enlarged the left visual from the earlier sparse lower placement, added all three missing lower details, shifted the brand to the accepted x≈80 position, and restored the concept's taller form rhythm. The 58/42 subpixel split still rasterizes at x=835. No fixable composition deviation remains. |
| Dashboard shell | Accepted composition and copy, normalized to a 1440×960 PNG with an exact 224px navigation region, exact 64px white top bar, exact menu order, and dense content canvas. | Final screenshot `/private/tmp/admin-task7-dashboard-1440-fix1.png` is 1440×960. Measured geometry remains an exact 224px sidebar and 64px header; the seven-item menu and five page regions retain their accepted order, all content fits without horizontal overflow, and the primary panels measure 607.61/552.39px (1.10:1), matching the concept's near-even composition. | Fix round 1 corrected the earlier 1.55:1 primary chart split. Approved content deviations remain explicit: the username comes from the static session, and Task 3 supplies `岗位采集、清洗与质量概况` rather than the concept's illustrative description. No shell or region item is missing or reordered. |
| Typography | Accepted by the user: compact Chinese admin typography with restrained heading, body, table, and metric scales. | Browser-computed desktop sizes are 24px page/form headings, 14px body copy, 16px section headings, 30px metrics, and 12px table/caption text. At 1024px and 390px the page heading remained one line (31.2px and 27.3px measured line boxes respectively), and chart/table labels remained readable. | No fixable deviation. The implementation uses the approved system Chinese font stack so platform glyph metrics may differ slightly from the raster concept. |
| Palette and borders | Accepted exact tokens and roles: `#087F7A` primary actions, selection, and primary charts; `#093B4B` sidebar and brand field; `#F3F7F8` page background; `#FFFFFF` panels and form field; `#16323D` main text; `#647B84` secondary text; `#91A3AA` weak text; `#DCE6E9` borders and dividers; `#138A63` success; `#D98B22` warning; `#C94C4C` exception; `#397A9F` information. | Final browser-computed evidence is exact: tokens `#087f7a` / `#093b4b`, primary `rgb(8, 127, 122)`, navigation `rgb(9, 59, 75)`, white surfaces, and `rgb(220, 230, 233)` borders. Normal 12px semantic/weak labels use approved `#16323D`, which regression calculations verify above 4.5:1 on both white and page backgrounds; semantic colors remain on indicators, borders, and icons. | No token change was required. The earlier screenshot-pixel samples (`rgb(56,125,122)` / `rgb(25,58,75)`) are capture/antialiasing samples at blended edges, not computed flat-region colors; computed CSS evidence exactly matches the accepted palette. |
| Charts and data density | Accepted by the user: four metrics, seven-day trend, source donut, city bars, quality overview, task table, activity table, and visible demonstration-data labeling. | The final renders include four 56px bordered metric-icon treatments using approved primary/information/warning colors. The trend spans its panel with dashed grids, filled points, and point-value labels; the larger donut and square legend markers align with the concept. All regions remain readable and semantically exposed, and the actual `today-tasks` metric now correctly reads `↑ 2 项 进行中`. | Fix round 1 added the missing metric-card icons and aligned the primary chart treatments/proportions. Final review added explicit metric change units without changing Task 3 values. The concept values remain illustrative; all static-only behavior is retained. |
| Responsive behavior | Both accepted desktop concepts now establish a deterministic 1440px-wide reference. Responsive behavior follows the approved written spec. | Final evidence is `/private/tmp/admin-task7-dashboard-1024-final.png` (1024×960) and `/private/tmp/admin-task7-dashboard-390-final.png` (390×844), both inspected directly with `view_image`. They include the final icons, chart treatments, accessible dark label text, and `2 项` presentation, with no page-width clipping or heading wrap; metric/charts stack and tables remain internally scrollable at 390px. Compiled layout behavior verifies drawer focus entry, Escape dismissal, focus return, and workspace/drawer inert plus ARIA isolation. | The stale pre-icon responsive captures are superseded by the two final artifacts. No unresolved responsive deviation remains. Mobile intentionally adds the compact `GoWorking 管理端` brand inside the form after the desktop visual field is hidden. |

## Above-the-fold copy diff

- Login at 1440px: no visible copy was added, removed, renamed, or reordered relative to the accepted concept.
- Dashboard at 1440px: shell/navigation labels, page title, metric labels, section titles, `演示数据`, update time, administrator label, and logout label retain their accepted order. The Task 3 contract intentionally replaces the concept's illustrative description, metric values, source names/percentages, and associated delta text as recorded above; no product claim or page region was added or removed.
