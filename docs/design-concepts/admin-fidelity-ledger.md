# Admin interface fidelity ledger

| Area | Accepted concept evidence | Implementation evidence | Fix or deviation |
| --- | --- | --- | --- |
| Login composition | Accepted composition and copy, normalized to a 1440×840 PNG. The raster divider is fixed at x=835: 835px left and 605px right, the nearest whole-pixel representation of 58/42. The form remains centered without a floating card or decorative badge. | Pending implementation. | Deterministic no-crop normalization from 1642×958: source left 963px → final 835px; source right 679px → final 605px; source height 958px → final 840px. |
| Dashboard shell | Accepted composition and copy, normalized to a 1440×960 PNG with an exact 224px navigation region, exact 64px white top bar, exact menu order, and dense content canvas. | Pending implementation. | Deterministic no-crop normalization from 1536×1024: sidebar 232px → 224px; content 1304px → 1216px; header remains 64px; body 960px → 896px. |
| Typography | Accepted by the user: compact Chinese admin typography with restrained heading, body, table, and metric scales. | Pending implementation. | None recorded. |
| Palette and borders | Accepted exact tokens and roles: `#087F7A` primary actions, selection, and primary charts; `#093B4B` sidebar and brand field; `#F3F7F8` page background; `#FFFFFF` panels and form field; `#16323D` main text; `#647B84` secondary text; `#91A3AA` weak text; `#DCE6E9` borders and dividers; `#138A63` success; `#D98B22` warning; `#C94C4C` exception; `#397A9F` information. | Pending implementation. | Visible tonal ramps were removed: teal, navigation, chart, icon, action, and semantic surfaces are flat approved colors. The earlier purple task icon remains corrected to `#087F7A`. |
| Charts and data density | Accepted by the user: four metrics, seven-day trend, source donut, city bars, quality overview, task table, activity table, and visible demonstration-data labeling. | Pending implementation. | None recorded. |
| Responsive behavior | Both accepted desktop concepts now establish a deterministic 1440px-wide reference. Responsive behavior follows the approved written spec. | Pending implementation. | Verify collapsed navigation at 1024px and mobile layout at 390px during browser QA. |
