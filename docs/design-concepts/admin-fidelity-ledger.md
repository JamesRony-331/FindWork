# Admin interface fidelity ledger

| Area | Accepted concept evidence | Implementation evidence | Fix or deviation |
| --- | --- | --- | --- |
| Login composition | Accepted composition and copy, normalized to a 1440×840 PNG. The raster divider is fixed at x=835: 835px left and 605px right, the nearest whole-pixel representation of 58/42. The form remains centered without a floating card or decorative badge. | In-app Browser screenshot `/private/tmp/admin-task7-login-1440.png` is 1440×840. Browser geometry measured the visual/form split at 835.20/604.80px and the centered form at 408px. The complete visible desktop copy and unchecked remember state match the accepted concept; empty-submit errors and the password visibility control were also verified. | No fixable deviation. The subpixel split is the browser's exact 58/42 calculation and rasterizes at the accepted x=835 divider. Deterministic no-crop normalization remains: source left 963px → final 835px; source right 679px → final 605px; source height 958px → final 840px. |
| Dashboard shell | Accepted composition and copy, normalized to a 1440×960 PNG with an exact 224px navigation region, exact 64px white top bar, exact menu order, and dense content canvas. | In-app Browser screenshot `/private/tmp/admin-task7-dashboard-1440-final.png` is 1440×960. Measured geometry is an exact 224px sidebar and 64px header; the seven-item menu order is exact, the five page regions remain in the accepted order, and all regions fit the dense desktop canvas without horizontal overflow. The desktop sidebar toggles between 224px and 72px with matching workspace offset and ARIA state. | Approved content deviations are explicit: the concept's illustrative username is supplied by the static login session; the page description is the Task 3 `dashboardMeta.description` (`岗位采集、清洗与质量概况`) rather than the concept's illustrative `集中查看岗位、采集任务与数据质量`. No item was reordered. Deterministic normalization remains: sidebar 232px → 224px; content 1304px → 1216px; header remains 64px; body 960px → 896px. |
| Typography | Accepted by the user: compact Chinese admin typography with restrained heading, body, table, and metric scales. | Browser-computed desktop sizes are 24px page/form headings, 14px body copy, 16px section headings, 30px metrics, and 12px table/caption text. At 1024px and 390px the page heading remained one line (31.2px and 27.3px measured line boxes respectively), and chart/table labels remained readable. | No fixable deviation. The implementation uses the approved system Chinese font stack so platform glyph metrics may differ slightly from the raster concept. |
| Palette and borders | Accepted exact tokens and roles: `#087F7A` primary actions, selection, and primary charts; `#093B4B` sidebar and brand field; `#F3F7F8` page background; `#FFFFFF` panels and form field; `#16323D` main text; `#647B84` secondary text; `#91A3AA` weak text; `#DCE6E9` borders and dividers; `#138A63` success; `#D98B22` warning; `#C94C4C` exception; `#397A9F` information. | Computed Browser styles confirmed `rgb(8, 127, 122)` primary, `rgb(9, 59, 75)` navigation, white surfaces, and `rgb(220, 230, 233)` borders. Direct concept/screenshot inspection found flat fills, restrained 6/10px radii, one-pixel borders, small panel shadows, and consistent current-color icon treatment. | No fixable deviation. Visible tonal ramps remain removed: teal, navigation, chart, icon, action, and semantic surfaces use approved colors. The source donut uses flat approved-color segments; no decorative gradient or glow was introduced. |
| Charts and data density | Accepted by the user: four metrics, seven-day trend, source donut, city bars, quality overview, task table, activity table, and visible demonstration-data labeling. | The 1440 screenshot contains four metrics, seven dated trend values, four source shares, six city bars, four quality rows, four collection tasks, four recent activities, the alert summary, and visible `演示数据` labeling. DOM inspection verified every chart label/value, table header, status, and alert count; semantic lists preserve the chart values independently of color. | The concept numbers/source names/task rows are illustrative. The implementation intentionally uses the approved Task 3 static datasets, including metric delta copy and four quality rows; trend values are listed below the SVG rather than annotated over points. These are recorded contract deviations, not omitted or reordered regions. |
| Responsive behavior | Both accepted desktop concepts now establish a deterministic 1440px-wide reference. Responsive behavior follows the approved written spec. | At 1024×960 the sidebar initialized at 72px, expanded to 224px, collapsed again, and the document stayed exactly 1024px wide. At 390×844 the document stayed exactly 390px wide, the mobile drawer opened and closed through its scrim, metrics/charts stacked, and the two table scrollers measured 330/640px and 330/560px client/scroll width. Mobile evidence: `/private/tmp/admin-task7-dashboard-390.png` and `/private/tmp/admin-task7-login-390.png`; the latter hides the decorative field and keeps a 350px form. | No unresolved responsive deviation. Mobile intentionally adds the compact `GoWorking 管理端` brand inside the form after the desktop visual field is hidden; this is responsive-only copy, not a desktop above-the-fold change. |

## Above-the-fold copy diff

- Login at 1440px: no visible copy was added, removed, renamed, or reordered relative to the accepted concept.
- Dashboard at 1440px: shell/navigation labels, page title, metric labels, section titles, `演示数据`, update time, administrator label, and logout label retain their accepted order. The Task 3 contract intentionally replaces the concept's illustrative description, metric values, source names/percentages, and associated delta text as recorded above; no product claim or page region was added or removed.
