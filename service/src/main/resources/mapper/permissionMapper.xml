<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wzbc.GoWorking.Mapper.PermissionMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="permissionPO.id">
        INSERT INTO sys_permission (permission_name, permission_code)
        VALUES (#{permissionPO.permissionName}, #{permissionPO.permissionCode})
    </insert>
    <update id="updateById">
        UPDATE sys_permission
        SET permission_name = #{permissionPO.permissionName}, permission_code = #{permissionPO.permissionCode}
        WHERE id = #{permissionPO.id}
    </update>
    <delete id="deleteById">
        DELETE FROM sys_permission WHERE id = #{id}
    </delete>
    <select id="selectById" resultType="com.wzbc.GoWorking.entity.PO.RBAC.PermissionPO">
        SELECT id, permission_name AS permissionName, permission_code AS permissionCode
        FROM sys_permission WHERE id = #{id}
    </select>
    <select id="selectAll" resultType="com.wzbc.GoWorking.entity.PO.RBAC.PermissionPO">
        SELECT id, permission_name AS permissionName, permission_code AS permissionCode
        FROM sys_permission ORDER BY id
    </select>
</mapper>
